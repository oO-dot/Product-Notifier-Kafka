server:
  port: 0
spring:
  kafka:
    consumer:
      bootstrap-servers: localhost:9092,localhost:9094
      group-id: product-created-events
      properties:
        spring:
          json:
            trusted:
              packages: 'ru.kafkademo.ws.core'